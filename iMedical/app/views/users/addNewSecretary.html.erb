<%= form_tag(add_new_doctor_path, method: :get, id: "owDoct-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search ..." %>
<% end %>
<table>
    <% @secretaries.each do |secretary| %>
        <tr>
            <td><%= secretary.name %></td>
        </tr>
    <% end %>
</table>
