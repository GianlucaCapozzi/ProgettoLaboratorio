<h2> Lista dottori che lavorano in una determinata clinica di un certo proprietario (Vista proprietario)</h2>

<table border="1">
	<% @doctors.each do |doctor| %>
		<tr>
			<td> <%= button_to doctor.surname + " " + doctor.name, owner_clinic_doctor_path(current_user.id, @clinic, doctor), method: :get %> </td>
		</tr>
	<% end %>
</table>
