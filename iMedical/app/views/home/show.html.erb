<!DOCTYPE html>
<html>
  <head>
    <h1>HOME PAGE</h1>
  </head>
  <body>
    <div>
        <h2>Signed in as <strong><%= current_user.email %></strong>!</h2>
        <br/>
    </div>
    <br/>
    <h1>Scegli la tua tipologia di utente</h1>
        <div class="container">
            <div class="row">
                <div class="col-sm-6">
                    <%= button_to "Proprietario", new_owner_path(current_user.id), :method => "get", class: "btn" %>
                </div>
                <div class="col-sm-6 text-right">
                    <%= button_to "Dottore", new_doctor_path(current_user.id), :method => "get", class: "btn" %>
                </div>
            </div>
        </div>
        <br/>
        <div class="container">
            <div class="row">
                <div class="col-sm-6">
                    <%= button_to "Paziente", new_patient_path(current_user.id), :method => "get", class: "btn" %>
                </div>
                <div class="col-sm-6 text-right">
                    <%= button_to "Segretario", new_secretary_path(current_user.id), :method => "get", class: "btn" %>
                </div>
            </div>
        </div>
    </div>
  </body>
</html>
